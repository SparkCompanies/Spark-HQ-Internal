/* patch-hq-celeb-card.cjs — Celebrations card on the Spark HQ home page.
   Injects a self-contained card under the welcome bar (below the Spark Standard card when present):
   today's birthdays and anniversaries, then the whole month's, dates already passed shown dimmed.
   SVG icons, no emojis. Renders nothing at all when there is nothing to celebrate, and a failed
   fetch is swallowed so the homepage can never break because of this card.
   Run from repo root:  node patch-hq-celeb-card.cjs
*/
const fs = require('fs');
const F = "index.html";
function read(f){ if(!fs.existsSync(f)) throw new Error('Cannot find '+f+' - run from the repo root.'); return fs.readFileSync(f,'utf8').replace(/\r\n/g,'\n'); }
function repl(s, oldStr, newStr, label){
  const n = s.split(oldStr).length - 1;
  if(n !== 1) throw new Error('ANCHOR '+label+': expected 1 match, found '+n+'. File differs from the copy I patched against. Nothing written.');
  return s.replace(oldStr, () => newStr);
}
let x = read(F);
if (x.indexOf("HQ_CELEBRATIONS_v1") !== -1) throw new Error('Already applied. Aborting.');
x = repl(x, "<script>/*STD_UNDER_TRAINING_v1*/", "<script>/*HQ_CELEBRATIONS_v1 — birthdays & anniversaries card on the home welcome bar*/\n(function () {\n  var WK = \"https://spark-hq-worker.sparkcompanies.workers.dev\";\n  var SVG = \"http://www.w3.org/2000/svg\";\n  function svg(d, stroke, fill) {\n    var s = document.createElementNS(SVG, \"svg\");\n    s.setAttribute(\"viewBox\", \"0 0 24 24\"); s.setAttribute(\"width\", \"18\"); s.setAttribute(\"height\", \"18\");\n    s.setAttribute(\"fill\", \"none\"); s.setAttribute(\"stroke\", stroke || \"#B8912E\");\n    s.setAttribute(\"stroke-width\", \"1.7\"); s.setAttribute(\"stroke-linecap\", \"round\"); s.setAttribute(\"stroke-linejoin\", \"round\");\n    s.style.flex = \"0 0 auto\";\n    d.split(\"|\").forEach(function (p) {\n      var el = document.createElementNS(SVG, \"path\");\n      el.setAttribute(\"d\", p); if (fill) el.setAttribute(\"fill\", fill);\n      s.appendChild(el);\n    });\n    return s;\n  }\n  /* cake */\n  var D_CAKE = \"M4 21h16|M5 21v-6a2 2 0 0 1 2-2h10a2 2 0 0 1 2 2v6|M8 13V9|M12 13V8|M16 13V9|M8 6.5c0-1 1-1.6 1-2.5 0 .9 1 1.5 1 2.5a1 1 0 0 1-2 0Z|M15 6.5c0-1 1-1.6 1-2.5 0 .9 1 1.5 1 2.5a1 1 0 0 1-2 0Z\";\n  /* award ribbon */\n  var D_AWARD = \"M12 3a5 5 0 1 0 0 10 5 5 0 0 0 0-10Z|M8.5 12.5 7 21l5-2.5L17 21l-1.5-8.5\";\n  function esc(s) { return String(s == null ? \"\" : s).replace(/[&<>\\\"]/g, function (c) { return { \"&\": \"&amp;\", \"<\": \"&lt;\", \">\": \"&gt;\", '\"': \"&quot;\" }[c]; }); }\n  function ord(n) { var s = [\"th\", \"st\", \"nd\", \"rd\"], v = n % 100; return n + (s[(v - 20) % 10] || s[v] || s[0]); }\n  function row(icon, html, dim) {\n    var r = document.createElement(\"div\");\n    r.style.cssText = \"display:flex;align-items:center;gap:9px;padding:5px 0;font-size:13.5px;color:\" + (dim ? \"#9a948a\" : \"#171614\") + \";line-height:1.45\";\n    r.appendChild(icon); \n    var t = document.createElement(\"div\"); t.innerHTML = html; t.style.flex = \"1\";\n    r.appendChild(t); return r;\n  }\n  function section(label) {\n    var h = document.createElement(\"div\");\n    h.textContent = label;\n    h.style.cssText = \"font-size:10px;letter-spacing:.16em;text-transform:uppercase;font-weight:700;color:#B8912E;margin:12px 0 4px\";\n    return h;\n  }\n  function build(j) {\n    var card = document.createElement(\"div\");\n    card.id = \"hqCelebCard\";\n    card.style.cssText = \"background:#fff;border:1px solid #e7e2d6;border-radius:14px;padding:16px 18px;margin:14px 0;box-shadow:0 2px 10px rgba(23,22,20,.06)\";\n    var head = document.createElement(\"div\");\n    head.style.cssText = \"display:flex;align-items:center;gap:10px;margin-bottom:2px\";\n    head.appendChild(svg(D_AWARD, \"#B8912E\"));\n    var ht = document.createElement(\"div\");\n    ht.innerHTML = \"<div style=\\\"font-weight:700;font-size:15.5px;color:#171614\\\">Celebrations</div>\";\n    head.appendChild(ht);\n    card.appendChild(head);\n    var today = (j.today || []);\n    if (today.length) {\n      card.appendChild(section(\"Today\"));\n      today.forEach(function (e) {\n        if (e.type === \"anniversary\") {\n          card.appendChild(row(svg(D_AWARD, \"#B8912E\"), \"<strong>\" + esc(e.name) + \"</strong> &middot; \" + e.years + \" year\" + (e.years === 1 ? \"\" : \"s\") + (e.company ? \" with \" + esc(e.company) : \"\")));\n        } else {\n          card.appendChild(row(svg(D_CAKE, \"#D4A017\"), \"Happy birthday, <strong>\" + esc(e.first) + \"</strong>\" + (e.company ? \" &middot; \" + esc(e.company) : \"\")));\n        }\n      });\n    }\n    var mb = (j.monthBirthdays || []);\n    if (mb.length) {\n      card.appendChild(section((j.monthName || \"This month\") + \" birthdays\"));\n      mb.forEach(function (e) {\n        card.appendChild(row(svg(D_CAKE, e.past ? \"#c9c3b7\" : \"#D4A017\"), \"<strong>\" + esc(e.name) + \"</strong> &middot; \" + ord(e.day) + (e.today ? \" &middot; today\" : \"\") + (e.company ? \" &middot; \" + esc(e.company) : \"\"), e.past));\n      });\n    }\n    var ma = (j.monthAnniversaries || []);\n    if (ma.length) {\n      card.appendChild(section((j.monthName || \"This month\") + \" anniversaries\"));\n      ma.forEach(function (e) {\n        card.appendChild(row(svg(D_AWARD, e.past ? \"#c9c3b7\" : \"#B8912E\"), \"<strong>\" + esc(e.name) + \"</strong> &middot; \" + e.years + \" year\" + (e.years === 1 ? \"\" : \"s\") + (e.company ? \" with \" + esc(e.company) : \"\") + \" &middot; \" + ord(e.day) + (e.today ? \" &middot; today\" : \"\"), e.past));\n      });\n    }\n    return card;\n  }\n  function mount(j) {\n    /* nothing to celebrate: render nothing at all rather than an empty box */\n    if (!j || !j.ok) return;\n    var any = (j.today || []).length || (j.monthBirthdays || []).length || (j.monthAnniversaries || []).length;\n    if (!any) return;\n    var bar = document.getElementById(\"lmsWelcomeBar\");\n    if (!bar || document.getElementById(\"hqCelebCard\")) return;\n    var card = build(j);\n    /* sit below the Spark Standard card when it is present, else right under the welcome bar */\n    var anchor = document.getElementById(\"stdCard\") || bar;\n    anchor.parentNode.insertBefore(card, anchor.nextSibling);\n  }\n  function load() {\n    if (!(window.SPARK_SB && window.SPARK_SB.getToken)) return;\n    window.SPARK_SB.getToken().then(function (tok) {\n      if (!tok) return;\n      return fetch(WK + \"/hq-celebrations\", { headers: { Authorization: \"Bearer \" + tok } })\n        .then(function (r) { return r.json(); })\n        .then(mount);\n    }).catch(function () { /* a quiet failure here must never break the homepage */ });\n  }\n  function go() {\n    var tries = 0;\n    var iv = setInterval(function () {\n      tries++;\n      if (document.getElementById(\"lmsWelcomeBar\") && window.SPARK_SB && window.SPARK_SB.getToken) { clearInterval(iv); load(); }\n      else if (tries > 40) clearInterval(iv);\n    }, 250);\n  }\n  if (document.readyState !== \"loading\") go(); else document.addEventListener(\"DOMContentLoaded\", go);\n})();\n</script>\n<script>/*STD_UNDER_TRAINING_v1*/", "celebrations card script");
fs.writeFileSync(F + ".bak-HQCELEBRATIO", read(F));
fs.writeFileSync(F, x);
console.log("OK  Celebrations card injected on the home welcome bar");
console.log("OK  today first, then the whole month; passed dates dimmed; hides itself when empty");
console.log("Backup: index.html.bak-HQCELEBRATI");
