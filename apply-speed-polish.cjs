// SPEED_POLISH_v1 — kills the perceived lag and fixes the band's layout.
// LAG: the KPI strip and calendar band each waited on a live network round-trip
//   before showing anything (Salesforce aggregates are the slowest call on the
//   page — that's why three KPIs sat as dashes). Now the last-known values are
//   painted INSTANTLY from localStorage on load, and the live fetch refreshes
//   them in the background. First-ever visit shows a shimmer, not dashes.
// LOOK: the band was one narrow column of text beside a field of whitespace.
//   Now it's two columns — agenda left, a mini month-at-a-glance rail on the
//   right (event days marked, today ringed in gold, "N meetings in the next
//   7 days"). Rows tighter (9px), capped at 5, day numbers 18px.
// CRLF-safe, idempotent. Run from the repo root, then commit + push.
const fs = require("fs");
const F = "spark-home.html";
const raw = fs.readFileSync(F, "utf8");
if (raw.includes("SPEED_POLISH_v1")) { console.log("Already applied."); process.exit(0); }
if (!raw.includes("CAL_BAND_v1")) { console.error("ABORT — CAL_BAND_v1 not present"); process.exit(1); }
const hadCRLF = /\r\n/.test(raw);
let h = raw.replace(/\r\n/g, "\n");
function die(m){ console.error("ABORT — " + m + " (no changes written)"); process.exit(1); }
const P = [["SF", "wGet('/sf-board').then(function(d){\n try{console.log('[Home] /sf-board keys:',Object.keys(d));}catch(e){}\n var pipe=d.pipelineTotal||d.openPipeline||d.pipeline||(d.totals&&(d.totals.pipeline||d.totals.openPipeline));\n var hc=d.headcount||d.onPayroll||(d.totals&&d.totals.headcount);\n var hrs=d.hours||d.weekHours||(d.totals&&d.totals.hours);\n if(pipe!=null){var p=num(pipe);if(p!=null)setKpi('kpiPipe',p>=1e6?'$'+(p/1e6).toFixed(2)+'M':'$'+Math.round(p/1e3)+'K');}\n if(hc!=null&&num(hc)!=null)setKpi('kpiHc',String(Math.round(num(hc))));\n if(hrs!=null&&num(hrs)!=null)setKpi('kpiHrs',Math.round(num(hrs)).toLocaleString());\n window.__SFSUM=d; /* HOME_LIVE_v1 \u2014 stash for live widgets + patch mounted cards */\n try{var _pa=document.getElementById('pipeLiveAmt');if(_pa&&d.pipelineTotal!=null)_pa.textContent='$'+(d.pipelineTotal/1e6).toFixed(2)+'M';\n var _hn=document.getElementById('hcLiveN');if(_hn&&d.headcount!=null)_hn.textContent=String(Math.round(d.headcount));\n var _hh=document.getElementById('hcLiveHrs');if(_hh&&d.hours!=null)_hh.textContent=Math.round(d.hours).toLocaleString();\n var _hw=document.getElementById('hcLiveWk');if(_hw&&d.weekEnding)_hw.textContent='on payroll \\u00b7 week ending '+d.weekEnding;}catch(_){}\n}).catch(function(e){/* HOME_DEFAKE_v1 \u2014 leave KPIs blank rather than show fake numbers */try{document.querySelectorAll('.kpi .big[data-live=\"sf\"]').forEach(function(el){if(el.textContent==='\\u2014'){el.title='Connects when the Salesforce board endpoint is live';}});}catch(_){}console.log('[Home] /sf-board unavailable \u2014 payroll/pipeline/hours left blank (no fake fallback):',e);});", "/* SPEED_POLISH_v1 \u2014 paint last-known numbers instantly, refresh in background */\nfunction hqCacheGet(k){try{var o=JSON.parse(localStorage.getItem(k)||'null');if(o&&o.t&&(Date.now()-o.t)<86400000)return o.d;}catch(e){}return null;}\nfunction hqCacheSet(k,d){try{localStorage.setItem(k,JSON.stringify({t:Date.now(),d:d}));}catch(e){}}\nfunction applySfBoard(d){\n try{console.log('[Home] /sf-board keys:',Object.keys(d));}catch(e){}\n var pipe=d.pipelineTotal||d.openPipeline||d.pipeline||(d.totals&&(d.totals.pipeline||d.totals.openPipeline));\n var hc=d.headcount||d.onPayroll||(d.totals&&d.totals.headcount);\n var hrs=d.hours||d.weekHours||(d.totals&&d.totals.hours);\n if(pipe!=null){var p=num(pipe);if(p!=null)setKpi('kpiPipe',p>=1e6?'$'+(p/1e6).toFixed(2)+'M':'$'+Math.round(p/1e3)+'K');}\n if(hc!=null&&num(hc)!=null)setKpi('kpiHc',String(Math.round(num(hc))));\n if(hrs!=null&&num(hrs)!=null)setKpi('kpiHrs',Math.round(num(hrs)).toLocaleString());\n window.__SFSUM=d; /* HOME_LIVE_v1 \u2014 stash for live widgets + patch mounted cards */\n try{var _pa=document.getElementById('pipeLiveAmt');if(_pa&&d.pipelineTotal!=null)_pa.textContent='$'+(d.pipelineTotal/1e6).toFixed(2)+'M';\n var _hn=document.getElementById('hcLiveN');if(_hn&&d.headcount!=null)_hn.textContent=String(Math.round(d.headcount));\n var _hh=document.getElementById('hcLiveHrs');if(_hh&&d.hours!=null)_hh.textContent=Math.round(d.hours).toLocaleString();\n var _hw=document.getElementById('hcLiveWk');if(_hw&&d.weekEnding)_hw.textContent='on payroll \\u00b7 week ending '+d.weekEnding;}catch(_){}\n}\nvar __sfCached=hqCacheGet('hq_sf_board');\nif(__sfCached){try{applySfBoard(__sfCached);}catch(e){}}\nelse{document.querySelectorAll('.kpi .big[data-live=\"sf\"]').forEach(function(el){el.innerHTML='<span class=\"kshim\"></span>';});}\nwGet('/sf-board').then(function(d){hqCacheSet('hq_sf_board',d);applySfBoard(d);}).catch(function(e){\nif(!window.__SFSUM){document.querySelectorAll('.kpi .big[data-live=\"sf\"]').forEach(function(el){el.textContent='\\u2014';});/* HOME_DEFAKE_v1 \u2014 leave KPIs blank rather than show fake numbers */try{document.querySelectorAll('.kpi .big[data-live=\"sf\"]').forEach(function(el){if(el.textContent==='\\u2014'){el.title='Connects when the Salesforce board endpoint is live';}});}catch(_){}console.log('[Home] /sf-board unavailable \u2014 payroll/pipeline/hours left blank (no fake fallback):',e);}\nelse{console.log('[Home] /sf-board refresh failed \\u2014 showing cached values:',e);}\n});"], ["CAL", "wGet('/calendar?days=45').then(function(d){\n window.__CALDAY=d; window.__CALMONTH=d;\n calBandRender(d);\n var el=document.getElementById('todayLiveList');\n if(el)el.innerHTML=todayRows(d);\n if(typeof pulseCalRender==='function') pulseCalRender(d);\n}).catch(function(e){\n var b=document.getElementById('cbBody');\n if(b)b.innerHTML='<div class=\"cbempty\">Calendar unavailable right now.</div>';\n var el=document.getElementById('todayLiveList');\n if(el)el.innerHTML='<div class=\"sub\">Calendar unavailable right now</div>';\n console.log('[Home] /calendar unavailable:',e);\n});", "function applyCalData(d){\n window.__CALDAY=d; window.__CALMONTH=d;\n calBandRender(d);\n var el=document.getElementById('todayLiveList');\n if(el)el.innerHTML=todayRows(d);\n if(typeof pulseCalRender==='function') pulseCalRender(d);\n}\nvar __calCached=hqCacheGet('hq_cal45'); /* SPEED_POLISH_v1: instant paint */\nif(__calCached){try{applyCalData(__calCached);}catch(e){}}\nwGet('/calendar?days=45').then(function(d){hqCacheSet('hq_cal45',d);applyCalData(d);}).catch(function(e){\n if(!__calCached){\n  var b=document.getElementById('cbBody');\n  if(b)b.innerHTML='<div class=\"cbempty\">Calendar unavailable right now.</div>';\n  var el=document.getElementById('todayLiveList');\n  if(el)el.innerHTML='<div class=\"sub\">Calendar unavailable right now</div>';\n }\n console.log('[Home] /calendar unavailable:',e);\n});"], ["MARKUP", "      <div class=\"cbbody\" id=\"cbBody\"><div class=\"cbskel\"><div style=\"width:62%\"></div><div style=\"width:44%\"></div><div style=\"width:71%\"></div></div></div>\n      <div class=\"cbfoot\" id=\"cbFoot\" style=\"display:none;\"></div>", "      <div class=\"cbcols\">\n        <div class=\"cbbody\" id=\"cbBody\"><div class=\"cbskel\"><div style=\"width:62%\"></div><div style=\"width:44%\"></div><div style=\"width:71%\"></div></div></div>\n        <aside class=\"cbrail\" id=\"cbRail\"></aside>\n      </div>\n      <div class=\"cbfoot\" id=\"cbFoot\" style=\"display:none;\"></div>"], ["MINIFN", "function calBandRender(d){\n  var body=document.getElementById('cbBody'), foot=document.getElementById('cbFoot');\n  if(!body) return;", "function calMiniRender(d){\n  var rail=document.getElementById('cbRail'); if(!rail) return;\n  var now=new Date();\n  var MN=['January','February','March','April','May','June','July','August','September','October','November','December'];\n  var evs=((d&&d.events)||[]).filter(function(e){return e.start;});\n  var byDay={}, wk=0, t0=new Date(now.getFullYear(),now.getMonth(),now.getDate()), t7=new Date(t0.getTime()+7*864e5);\n  evs.forEach(function(e){\n    var s=new Date(e.start);\n    if(s>=t0&&s<t7) wk++;\n    if(s.getMonth()===now.getMonth()&&s.getFullYear()===now.getFullYear()) byDay[s.getDate()]=1;\n  });\n  var first=new Date(now.getFullYear(),now.getMonth(),1).getDay();\n  var days=new Date(now.getFullYear(),now.getMonth()+1,0).getDate();\n  var html='<div class=\"mlab\">'+MN[now.getMonth()]+' '+now.getFullYear()+'</div><table><tr><th>S</th><th>M</th><th>T</th><th>W</th><th>T</th><th>F</th><th>S</th></tr><tr>';\n  for(var b=0;b<first;b++) html+='<td></td>';\n  for(var day=1;day<=days;day++){\n    if((first+day-1)%7===0 && day!==1) html+='</tr><tr>';\n    var cls=[]; if(byDay[day])cls.push('evt'); if(day===now.getDate())cls.push('today');\n    html+='<td'+(cls.length?' class=\"'+cls.join(' ')+'\"':'')+'>'+day+'</td>';\n  }\n  html+='</tr></table><div class=\"mfoot\">'+wk+' meeting'+(wk===1?'':'s')+' in the next 7 days</div>';\n  rail.innerHTML=html;\n}\nfunction calBandRender(d){\n  var body=document.getElementById('cbBody'), foot=document.getElementById('cbFoot');\n  if(!body) return;\n  try{calMiniRender(d);}catch(e){}"], ["ROWCAP", "var show=rows.slice(0,6), out='', lastKey='';", "var show=rows.slice(0,5), out='', lastKey='';"]];
for (const [name, oldS] of P) {
  const n = h.split(oldS).length - 1;
  if (n !== 1) die(name + " anchor found " + n + " times (want 1)");
}
for (const [name, oldS, newS] of P) h = h.split(oldS).join(newS);
const CSS = "\n/* SPEED_POLISH_v1 \u2014 shimmer KPIs, tighter band, mini-month rail */\n.kshim{display:inline-block;width:58px;height:20px;border-radius:6px;background:linear-gradient(90deg,#f0eee6,#faf9f4,#f0eee6);background-size:200% 100%;animation:cbsh 1.1s linear infinite;vertical-align:middle;}\n.calband .cbcols{display:grid;grid-template-columns:1fr 236px;align-items:stretch;}\n.calband .cbrail{border-left:1px solid var(--line-light);padding:12px 16px 14px;background:#fefdf9;}\n.calband .cbrail .mlab{font-size:10px;letter-spacing:.12em;color:var(--muted);text-transform:uppercase;margin-bottom:6px;font-weight:600;}\n.calband .cbrail table{width:100%;border-collapse:collapse;font-size:10.5px;text-align:center;}\n.calband .cbrail th{color:var(--muted);font-weight:500;padding:2px 0;}\n.calband .cbrail td{padding:3px 0;color:#4a463c;border-radius:5px;}\n.calband .cbrail td.evt{background:#fff3c2;font-weight:700;color:#6b5900;}\n.calband .cbrail td.today{outline:2px solid var(--gold);outline-offset:-2px;font-weight:800;color:#8a5c00;}\n.calband .cbrail .mfoot{margin-top:8px;font-size:10.5px;color:var(--muted);}\n.calband .cbrow{padding:9px 16px;gap:12px;}\n.calband .cbday{min-width:46px;}\n.calband .cbday .dnum{font-size:18px;}\n.calband .cbsub{font-size:13.5px;margin-bottom:2px;}\n.calband .cbhead{padding:11px 16px;}\n@media(max-width:900px){.calband .cbcols{grid-template-columns:1fr;}.calband .cbrail{display:none;}}\n";
const li = h.lastIndexOf("</style>");
if (li < 0) die("no </style>");
h = h.slice(0, li) + CSS + h.slice(li);
const stamp = new Date().toISOString().replace(/[:.]/g, "-");
fs.writeFileSync("spark-home.html.speed-" + stamp + ".bak", raw);
fs.writeFileSync(F, hadCRLF ? h.replace(/\n/g, "\r\n") : h);
console.log("APPLIED SPEED_POLISH_v1");
console.log("  KPIs + calendar paint instantly from your last visit; live refresh in background");
console.log("  shimmer instead of dashes on first-ever load");
console.log("  band: agenda left, mini month rail right, tighter rows (max 5)");
console.log("  NEXT: git add spark-home.html && git commit -m \"Home: instant paint from cache, mini-month rail, tighter band\" && git push");
