/* patch-bulk-set-column.cjs — set one column across every selected row, monday-style.
   Adds a "Set column" button to the floating selection bar. Pick a column, pick or type a value,
   Apply, and it writes to every selected row in one batched save.
   Design notes:
     - the toolbar route, not inline-on-blur: a 58-row overwrite should be one intentional click,
       not a side effect of leaving a cell.
     - a confirm appears at 10 rows or more, naming the column, the value and the count.
     - "Clear the value instead" empties the column on the selection.
     - rows that already hold the target value are skipped, so the save stays small.
     - each op carries the cell's previous value, so the worker rejects just the cells a colleague
       changed underneath you instead of clobbering their edit.
     - name and the Salesforce cell are deliberately excluded: bulk-renaming is nearly always a
       mistake, and the SF cell writes per candidate and must not fire in a loop.
     - ops are chunked at 200 per call, the server's documented limit.
   Run from repo root:  node patch-bulk-set-column.cjs
*/
const fs = require('fs');
const F = 'spark-boards.html';
function read(f){ if(!fs.existsSync(f)) throw new Error('Cannot find '+f+' - run from the repo root.'); return fs.readFileSync(f,'utf8').replace(/\r\n/g,'\n'); }
function repl(s, oldStr, newStr, label){
  const n = s.split(oldStr).length - 1;
  if(n !== 1) throw new Error('ANCHOR '+label+': expected 1 match, found '+n+'. Nothing written - send me the current spark-boards.html.');
  return s.replace(oldStr, () => newStr);
}
let x = read(F);
if (x.indexOf('bulkSetColumn') !== -1) throw new Error('Already applied. Aborting.');
x = repl(x, "/* Floating multi-select action bar (Monday-style) */", "/* injected: bulk column edit popover */\nconst _sbBulkCss = document.createElement('style');\n_sbBulkCss.textContent = \".bulkedit-pop{position:absolute;bottom:calc(100% + 10px);left:50%;transform:translateX(-50%);min-width:290px;max-width:340px;background:#fff;border:1px solid var(--line,#e7e2d6);border-radius:12px;box-shadow:0 12px 34px rgba(20,18,10,.22);padding:12px;z-index:99999;text-align:left}\"\n + \".bulkedit-pop h4{margin:0 0 9px;font-size:11px;letter-spacing:.14em;text-transform:uppercase;color:#9a948a;font-weight:800}\"\n + \".bulkedit-pop select,.bulkedit-pop input{width:100%;font:inherit;font-size:13px;padding:7px 9px;border:1px solid var(--line,#e7e2d6);border-radius:8px;background:#fff;color:#171614;margin-bottom:9px}\"\n + \".bulkedit-pop .be-row{display:flex;gap:7px;margin-top:2px}\"\n + \".bulkedit-pop button{flex:1;font:inherit;font-size:12.5px;font-weight:700;padding:8px 10px;border-radius:8px;border:1px solid var(--line,#e7e2d6);background:#fff;cursor:pointer}\"\n + \".bulkedit-pop button.go{background:var(--gold,#F5C518);border-color:var(--gold,#F5C518);color:#171614}\"\n + \".bulkedit-pop .be-note{font-size:11.5px;color:#6f6a5e;margin:0 0 9px;line-height:1.35}\"\n + \".bulkedit-pop .be-clear{font-size:11.5px;color:#9a948a;display:flex;align-items:center;gap:6px;margin:0 0 9px;cursor:pointer}\";\ndocument.head.appendChild(_sbBulkCss);\n\n/* Floating multi-select action bar (Monday-style) */", "bulk edit css");
x = repl(x, "function BulkBar({\n  count,\n  groups,\n  onDuplicate,\n  onMove,\n  onArchive,\n  onDelete,\n  onClear\n}) {\n  const [moveOpen, setMoveOpen] = useState(false);", "function BulkBar({\n  count,\n  groups,\n  columns,\n  onBulkSet,\n  onDuplicate,\n  onMove,\n  onArchive,\n  onDelete,\n  onClear\n}) {\n  const [moveOpen, setMoveOpen] = useState(false);\n  const [editOpen, setEditOpen] = useState(false);\n  /* which column types can be bulk set. name is excluded on purpose (renaming every\n     selected row to one value is nearly always a mistake) and so is the Salesforce\n     cell, which writes per candidate and must not fire in a loop. */\n  const BULK_TYPES = ['status', 'dropdown', 'priority', 'text', 'number', 'date', 'person', 'checkbox'];\n  const editable = (columns || []).filter(c => c && BULK_TYPES.indexOf(c.type) !== -1 && c.key !== 'name');\n  const [bcol, setBcol] = useState('');\n  const [bval, setBval] = useState('');\n  const [bclear, setBclear] = useState(false);\n  const curCol = editable.find(c => c.key === bcol) || null;\n  const optsFor = c => (c && c.options ? c.options : []).map((o, i) => ({ i: i, label: o.label }));\n  const apply = () => {\n    if (!curCol) return;\n    let val;\n    if (bclear) val = curCol.type === 'checkbox' ? false : curCol.type === 'number' ? 0 : ['status', 'dropdown', 'priority', 'person'].indexOf(curCol.type) !== -1 ? null : '';\n    else if (['status', 'dropdown', 'priority'].indexOf(curCol.type) !== -1) { if (bval === '') return; val = Number(bval); }\n    else if (curCol.type === 'checkbox') val = bval === 'true';\n    else if (curCol.type === 'number') { if (bval === '') return; val = Number(bval); if (isNaN(val)) return; }\n    else if (curCol.type === 'person') val = bval === '' ? null : bval;\n    else val = bval;\n    const label = bclear ? 'Clear' : ['status', 'dropdown', 'priority'].indexOf(curCol.type) !== -1 ? ((curCol.options || [])[val] || {}).label || String(val) : curCol.type === 'checkbox' ? (val ? 'checked' : 'unchecked') : String(val) === '' ? '(blank)' : String(val);\n    if (count >= 10 && !confirm('Set ' + (curCol.label || curCol.key) + ' to \"' + label + '\" on ' + count + ' rows?')) return;\n    onBulkSet(curCol.key, val);\n    setEditOpen(false); setBval(''); setBclear(false);\n  };", "BulkBar signature");
x = repl(x, "  })), /*#__PURE__*/React.createElement(\"span\", null, \"Archive\")), /*#__PURE__*/React.createElement(\"button\", {\n    className: \"bulk-act danger\",\n    onClick: onDelete\n  },", "  })), /*#__PURE__*/React.createElement(\"span\", null, \"Archive\")), editable.length > 0 && /*#__PURE__*/React.createElement(\"div\", {\n    style: { position: 'relative' }\n  }, /*#__PURE__*/React.createElement(\"button\", {\n    className: \"bulk-act\",\n    onClick: () => { setEditOpen(o => !o); setMoveOpen(false); },\n    title: \"Set one column on every selected row\"\n  }, /*#__PURE__*/React.createElement(\"svg\", {\n    viewBox: \"0 0 24 24\", width: \"17\", height: \"17\", fill: \"none\",\n    stroke: \"currentColor\", strokeWidth: \"1.8\", strokeLinecap: \"round\", strokeLinejoin: \"round\"\n  }, /*#__PURE__*/React.createElement(\"path\", {\n    d: \"M12 20h9M16.5 3.5a2.1 2.1 0 0 1 3 3L7 19l-4 1 1-4z\"\n  })), /*#__PURE__*/React.createElement(\"span\", null, \"Set column\")), editOpen && /*#__PURE__*/React.createElement(\"div\", {\n    className: \"bulkedit-pop\"\n  }, /*#__PURE__*/React.createElement(\"h4\", null, \"Set a column on \", count, \" \", count === 1 ? 'row' : 'rows'),\n    /*#__PURE__*/React.createElement(\"select\", {\n      value: bcol,\n      onChange: e => { setBcol(e.target.value); setBval(''); setBclear(false); }\n    }, /*#__PURE__*/React.createElement(\"option\", { value: \"\" }, \"Choose a column\\u2026\"),\n      editable.map(c => /*#__PURE__*/React.createElement(\"option\", { key: c.key, value: c.key }, c.label || c.key))),\n    curCol && /*#__PURE__*/React.createElement(\"label\", {\n      className: \"be-clear\"\n    }, /*#__PURE__*/React.createElement(\"input\", {\n      type: \"checkbox\", checked: bclear, style: { width: 'auto', margin: 0 },\n      onChange: e => setBclear(e.target.checked)\n    }), \"Clear the value instead\"),\n    curCol && !bclear && (['status', 'dropdown', 'priority'].indexOf(curCol.type) !== -1\n      ? /*#__PURE__*/React.createElement(\"select\", {\n          value: bval, onChange: e => setBval(e.target.value)\n        }, /*#__PURE__*/React.createElement(\"option\", { value: \"\" }, \"Choose a value\\u2026\"),\n          optsFor(curCol).map(o => /*#__PURE__*/React.createElement(\"option\", { key: o.i, value: o.i }, o.label)))\n      : curCol.type === 'checkbox'\n      ? /*#__PURE__*/React.createElement(\"select\", {\n          value: bval, onChange: e => setBval(e.target.value)\n        }, /*#__PURE__*/React.createElement(\"option\", { value: \"\" }, \"Choose\\u2026\"),\n          /*#__PURE__*/React.createElement(\"option\", { value: \"true\" }, \"Checked\"),\n          /*#__PURE__*/React.createElement(\"option\", { value: \"false\" }, \"Unchecked\"))\n      : /*#__PURE__*/React.createElement(\"input\", {\n          type: curCol.type === 'date' ? 'date' : curCol.type === 'number' ? 'number' : 'text',\n          value: bval, placeholder: 'New value', autoFocus: true,\n          onChange: e => setBval(e.target.value),\n          onKeyDown: e => { if (e.key === 'Enter') apply(); }\n        })),\n    curCol && /*#__PURE__*/React.createElement(\"p\", { className: \"be-note\" }, \"Applies to every selected row. Rows someone else changed in the meantime are skipped, not overwritten.\"),\n    /*#__PURE__*/React.createElement(\"div\", { className: \"be-row\" },\n      /*#__PURE__*/React.createElement(\"button\", { onClick: () => setEditOpen(false) }, \"Cancel\"),\n      /*#__PURE__*/React.createElement(\"button\", { className: \"go\", disabled: !curCol, onClick: apply }, \"Apply\"))\n  )), /*#__PURE__*/React.createElement(\"button\", {\n    className: \"bulk-act danger\",\n    onClick: onDelete\n  },", "bulk edit button");
x = repl(x, "  const bulkDuplicate = () => {", "  /* Set one column to one value across every selected row, in a single batched save.\n     Persist.cell carries each cell's previous value, so the worker can detect a row a\n     colleague changed underneath us and reject just that cell rather than clobbering it. */\n  const bulkSetColumn = (key, val) => {\n    const col = board.columns.find(c => c.key === key);\n    if (!col) return;\n    const nb = structuredClone(board);\n    const ops = [];\n    const me = sparkMe();\n    nb.groups.forEach(g => (g.items || []).forEach(it => {\n      if (!sel[it.id] || it.archivedAt || it.deletedAt) return;\n      const old = it[key];\n      if (String(old === undefined ? '' : old) === String(val === undefined ? '' : val)) return; /* already there */\n      it[key] = val;\n      ops.push({ itemId: it.id, key: key, val: val, was: old });\n    }));\n    if (!ops.length) { sbFlash('Nothing to change - every selected row already has that value.'); return; }\n    const shown = ['status', 'dropdown', 'priority'].indexOf(col.type) !== -1 ? (((col.options || [])[val] || {}).label || '(cleared)') : col.type === 'checkbox' ? (val ? 'checked' : 'unchecked') : (val === '' || val === null ? '(cleared)' : String(val));\n    logAct(nb, { actor: me, kind: 'bulk', col: key, colLabel: col.label || key, to: shown, count: ops.length, note: 'Set ' + (col.label || key) + ' to ' + shown + ' on ' + ops.length + ' rows' });\n    update(() => nb);\n    /* /boards-patch takes up to 200 cells per call, so chunk anything larger */\n    const CH = 200;\n    for (let i = 0; i < ops.length; i += CH) {\n      Persist.cell(nb, ops.slice(i, i + CH), i === 0 && nb.activity ? [nb.activity[0]] : []);\n    }\n    sbFlash('Set ' + (col.label || key) + ' to ' + shown + ' on ' + ops.length + (ops.length === 1 ? ' row' : ' rows') + '.', 'ok');\n    clearSel();\n  };\n  const bulkDuplicate = () => {", "bulkSetColumn handler");
x = repl(x, "    count: selIds.length,\n    groups: board.groups,\n    onDuplicate: bulkDuplicate,", "    count: selIds.length,\n    groups: board.groups,\n    columns: board.columns,\n    onBulkSet: bulkSetColumn,\n    onDuplicate: bulkDuplicate,", "BulkBar props");
fs.writeFileSync(F + '.bak-bulkset', read(F));
fs.writeFileSync(F, x);
console.log('OK  "Set column" added to the selection bar');
console.log('OK  status/dropdown/priority pick a label; text, number, date and checkbox type or choose');
console.log('OK  confirm at 10+ rows; unchanged rows skipped; conflicting rows skipped not overwritten');
console.log('OK  batched in chunks of 200 cells per save');
console.log('Backup: spark-boards.html.bak-bulkset');
