/* patch-import-xlsx.cjs — "Update from Excel": push an edited board export back onto the board.
   Export the board, edit it in Excel, then Update from Excel. Rows are matched to board items BY
   NAME and spreadsheet headers to board columns BY LABEL, so the export/edit/import loop round-trips.
   Before anything is written you see the full plan: how many cells change on how many rows, which
   names were not on the board, which columns were ignored, which values did not fit an option list,
   and the change list itself. OK applies it through the same conflict-checked path as normal edits.
   Safety rules baked in:
     - blank spreadsheet cells never clear a board value (blank means untouched, not erase)
     - Person and Salesforce columns are never written from a spreadsheet
     - unknown status labels are skipped and named, not guessed
     - names that appear twice on the board are skipped as ambiguous, not picked at random
     - this is an UPDATE, not a replace: rows not in the sheet are untouched, other groups untouched
     - Import JSON (bottom-right) remains the whole-board REPLACE for migrations; do not use it for this
   Run from repo root:  node patch-import-xlsx.cjs
*/
const fs = require('fs');
const F = 'spark-boards.html';
function read(f){ if(!fs.existsSync(f)) throw new Error('Cannot find '+f+' - run from the repo root.'); return fs.readFileSync(f,'utf8').replace(/\r\n/g,'\n'); }
function repl(s, oldStr, newStr, label){
  const n = s.split(oldStr).length - 1;
  if(n !== 1) throw new Error('ANCHOR '+label+': expected 1 match, found '+n+'. Nothing written - send me the current spark-boards.html.');
  return s.replace(oldStr, () => newStr);
}
let x = read(F);
if (x.indexOf('planXlsxUpdates') !== -1) throw new Error('Already applied. Aborting.');
x = repl(x, "  const wb = XLSX.utils.book_new();\n  XLSX.utils.book_append_sheet(wb, ws, board.name.slice(0, 28));\n  XLSX.writeFile(wb, board.name.replace(/[^\\w]+/g, '_') + '_' + todayShift(0) + '.xlsx');", "  const wb = XLSX.utils.book_new();\n  XLSX.utils.book_append_sheet(wb, ws, board.name.slice(0, 28));\n  XLSX.writeFile(wb, board.name.replace(/[^\\w]+/g, '_') + '_' + todayShift(0) + '.xlsx');\n/* ---------------- update from Excel (SheetJS) ----------------\n   Inverse of exportBoardToXlsx. Reads an exported-and-edited workbook, matches each row to a board\n   item BY NAME, maps spreadsheet headers to board columns BY LABEL, and produces a list of cell\n   changes. Nothing is written here: the caller shows the plan and, on confirm, applies it through\n   /boards-patch in chunks with each cell's previous value, so a row a colleague changed in the\n   meantime is rejected by the worker rather than overwritten.\n   Rules: blank spreadsheet cells never clear a board value (a blank almost always means\n   \"untouched\", not \"erase\"). Person and Salesforce cells are never written from a spreadsheet. */\nfunction sbNorm(s) { return String(s == null ? '' : s).replace(/\\s+/g, ' ').trim().toLowerCase(); }\nfunction planXlsxUpdates(board, aoa) {\n  const plan = { ops: [], changes: [], unmatched: [], ambiguous: [], ignoredCols: [], badValues: [], rowsSeen: 0, rowsMatched: 0 };\n  const cols = board.columns || [];\n  const byLabel = {};\n  cols.forEach(c => { if (c && c.label) byLabel[sbNorm(c.label)] = c; });\n  /* items by normalised name; keep all matches so we can flag ambiguity */\n  const byName = {};\n  (board.groups || []).forEach(g => (g.items || []).forEach(it => {\n    if (!it || it.archivedAt || it.deletedAt) return;\n    const k = sbNorm(it.name); if (!k) return;\n    (byName[k] = byName[k] || []).push(it);\n  }));\n  let map = null; /* [{ci, col}] for the current header */\n  const isHeader = row => row && row.some(c => ['item', 'name'].indexOf(sbNorm(c)) !== -1);\n  const NOWRITE = { person: 'people are matched by account, not spreadsheet text', sf: 'Salesforce link state is owned by the Sync' };\n  aoa.forEach(row => {\n    if (!row) return;\n    if (isHeader(row)) {\n      map = []; let nameIdx = -1;\n      row.forEach((h, ci) => {\n        const n = sbNorm(h);\n        if (n === 'item' || n === 'name') { if (nameIdx === -1) nameIdx = ci; return; }\n        if (!n) return;\n        const col = byLabel[n];\n        if (!col) { if (plan.ignoredCols.indexOf(String(h)) === -1) plan.ignoredCols.push(String(h) + ' (no such column)'); return; }\n        if (NOWRITE[col.type]) { const t = col.label + ' (' + NOWRITE[col.type] + ')'; if (plan.ignoredCols.indexOf(t) === -1) plan.ignoredCols.push(t); return; }\n        map.push({ ci: ci, col: col });\n      });\n      map.nameIdx = nameIdx;\n      return;\n    }\n    if (!map || map.nameIdx === -1) return;\n    const rawName = row[map.nameIdx];\n    const k = sbNorm(rawName);\n    if (!k) return; /* spacer, title or summary row */\n    plan.rowsSeen++;\n    const hits = byName[k] || [];\n    if (!hits.length) { plan.unmatched.push(String(rawName)); return; }\n    if (hits.length > 1) { plan.ambiguous.push(String(rawName)); return; }\n    const it = hits[0];\n    plan.rowsMatched++;\n    map.forEach(m => {\n      const col = m.col, raw = row[m.ci];\n      const txt = raw == null ? '' : String(raw).trim();\n      if (txt === '') return; /* blanks never clear */\n      let val, cur = it[col.key];\n      if (col.type === 'status') {\n        const i = (col.options || []).findIndex(o => sbNorm(o.label) === sbNorm(txt));\n        if (i === -1) { plan.badValues.push(it.name + ' · ' + col.label + ': \"' + txt + '\" is not a ' + col.label + ' option'); return; }\n        val = i; if (cur === val) return;\n      } else if (col.type === 'priority') {\n        const i = (typeof PRIORITIES !== 'undefined' ? PRIORITIES : []).findIndex(p => sbNorm(p.label) === sbNorm(txt));\n        if (i === -1) { plan.badValues.push(it.name + ' · ' + col.label + ': \"' + txt + '\" is not a priority'); return; }\n        val = i; if (cur === val) return;\n      } else if (col.type === 'checkbox') {\n        val = /^(x|yes|y|true|1|✓|✔)$/i.test(txt); if (!!cur === val) return;\n      } else if (col.type === 'number' || col.type === 'progress') {\n        const n = Number(String(txt).replace(/[%,]/g, ''));\n        if (isNaN(n)) { plan.badValues.push(it.name + ' · ' + col.label + ': \"' + txt + '\" is not a number'); return; }\n        val = n; if (Number(cur == null ? NaN : cur) === val) return;\n      } else if (col.type === 'date') {\n        let d = txt;\n        const m1 = txt.match(/^(\\d{4})-(\\d{2})-(\\d{2})/); const m2 = txt.match(/^(\\d{1,2})\\/(\\d{1,2})\\/(\\d{2,4})$/);\n        if (m1) d = m1[1] + '-' + m1[2] + '-' + m1[3];\n        else if (m2) { const y = m2[3].length === 2 ? '20' + m2[3] : m2[3]; d = y + '-' + String(m2[1]).padStart(2, '0') + '-' + String(m2[2]).padStart(2, '0'); }\n        else { const dt = new Date(txt); if (isNaN(dt)) { plan.badValues.push(it.name + ' · ' + col.label + ': \"' + txt + '\" is not a date'); return; } d = dt.toISOString().slice(0, 10); }\n        val = d; if (String(cur || '') === val) return;\n      } else {\n        val = txt; if (String(cur == null ? '' : cur).trim() === val) return;\n      }\n      plan.ops.push({ itemId: it.id, key: col.key, val: val, was: cur === undefined ? null : cur });\n      const shown = col.type === 'status' ? txt : col.type === 'checkbox' ? (val ? 'checked' : 'unchecked') : String(val);\n      const before = col.type === 'status' ? ((col.options || [])[cur] || {}).label || '(blank)' : cur == null || cur === '' ? '(blank)' : String(cur);\n      plan.changes.push({ item: it.name, col: col.label, from: before, to: shown });\n    });\n  });\n  return plan;\n}\nfunction readXlsxAoa(file) {\n  return new Promise((res, rej) => {\n    if (typeof XLSX === 'undefined') { rej(new Error('Spreadsheet library still loading. Try again in a second.')); return; }\n    const r = new FileReader();\n    r.onerror = () => rej(new Error('Could not read that file.'));\n    r.onload = () => {\n      try {\n        const wb = XLSX.read(new Uint8Array(r.result), { type: 'array', cellDates: false });\n        const ws = wb.Sheets[wb.SheetNames[0]];\n        res(XLSX.utils.sheet_to_json(ws, { header: 1, defval: null, raw: false }));\n      } catch (e) { rej(new Error('That does not look like an Excel file.')); }\n    };\n    r.readAsArrayBuffer(file);\n  });\n}\nfunction describeXlsxPlan(plan) {\n  const L = [];\n  L.push(plan.changes.length + ' cell' + (plan.changes.length === 1 ? '' : 's') + ' will change across ' + plan.rowsMatched + ' matched row' + (plan.rowsMatched === 1 ? '' : 's') + '.');\n  if (plan.unmatched.length) L.push('', 'NOT on this board (skipped): ' + plan.unmatched.slice(0, 8).join(', ') + (plan.unmatched.length > 8 ? ' +' + (plan.unmatched.length - 8) + ' more' : ''));\n  if (plan.ambiguous.length) L.push('', 'Name appears more than once on the board (skipped): ' + plan.ambiguous.join(', '));\n  if (plan.ignoredCols.length) L.push('', 'Columns ignored: ' + plan.ignoredCols.join('; '));\n  if (plan.badValues.length) L.push('', 'Values that did not fit (skipped): ' + plan.badValues.slice(0, 6).join(' | ') + (plan.badValues.length > 6 ? ' | +' + (plan.badValues.length - 6) + ' more' : ''));\n  if (plan.changes.length) {\n    L.push('', 'Changes:');\n    plan.changes.slice(0, 22).forEach(c => L.push('  ' + c.item + ' · ' + c.col + ': ' + c.from + ' → ' + c.to));\n    if (plan.changes.length > 22) L.push('  … and ' + (plan.changes.length - 22) + ' more');\n  }\n  L.push('', 'Blank spreadsheet cells never clear a board value.');\n  return L.join('\\n');\n}\n", "importer functions");
x = repl(x, "  }, /*#__PURE__*/React.createElement(\"button\", {\n    className: \"btn\",\n    title: \"Export to Excel\",\n    onClick: () => exportBoardToXlsx(board)\n  }, /*#__PURE__*/React.createElement(Ic.download, null), \"Export\"), canEdit && /*#__PURE__*/React.createElement(\"button\", {\n    className: \"btn\",\n    onClick: onOpenAutomations\n  },", "  }, /*#__PURE__*/React.createElement(\"button\", {\n    className: \"btn\",\n    title: \"Export to Excel\",\n    onClick: () => exportBoardToXlsx(board)\n  }, /*#__PURE__*/React.createElement(Ic.download, null), \"Export\"), canEdit && /*#__PURE__*/React.createElement(\"button\", {\n    className: \"btn\",\n    title: \"Update this board from an edited Excel export (matches rows by name)\",\n    onClick: () => {\n      const inp = document.createElement('input');\n      inp.type = 'file'; inp.accept = '.xlsx,.xls,.csv';\n      inp.onchange = () => {\n        const f = inp.files && inp.files[0]; if (!f) return;\n        const base = fullBoard || board;\n        readXlsxAoa(f).then(aoa => {\n          const plan = planXlsxUpdates(base, aoa);\n          if (!plan.rowsSeen) { alert('No rows found. The sheet needs a header row with \"Item\" or \"Name\" and the column labels from this board.'); return; }\n          if (!plan.changes.length) { alert('Nothing to update: every matched row already has these values.\\n\\n' + describeXlsxPlan(plan)); return; }\n          if (!confirm('Update \"' + base.name + '\" from ' + f.name + '?\\n\\n' + describeXlsxPlan(plan) + '\\n\\nOK = apply these changes.')) return;\n          const nb = structuredClone(base);\n          const idx = {}; nb.groups.forEach(g => (g.items || []).forEach(it => { idx[it.id] = it; }));\n          plan.ops.forEach(o => { if (idx[o.itemId]) idx[o.itemId][o.key] = o.val; });\n          logAct(nb, { actor: sparkMe(), kind: 'bulk', note: 'Updated ' + plan.ops.length + ' cells on ' + plan.rowsMatched + ' rows from ' + f.name, count: plan.ops.length });\n          update(() => nb);\n          const CH = 200;\n          for (let i = 0; i < plan.ops.length; i += CH) Persist.cell(nb, plan.ops.slice(i, i + CH), i === 0 && nb.activity ? [nb.activity[0]] : []);\n          sbFlash('Updated ' + plan.ops.length + ' cell' + (plan.ops.length === 1 ? '' : 's') + ' on ' + plan.rowsMatched + ' rows from ' + f.name + '.', 'ok');\n        }).catch(e => alert('Import stopped: ' + (e && e.message ? e.message : e)));\n      };\n      inp.click();\n    }\n  }, /*#__PURE__*/React.createElement(Ic.upload || Ic.download, null), \"Update from Excel\"), canEdit && /*#__PURE__*/React.createElement(\"button\", {\n    className: \"btn\",\n    onClick: onOpenAutomations\n  },", "import button");
fs.writeFileSync(F + '.bak-importxlsx', read(F));
fs.writeFileSync(F, x);
console.log('OK  "Update from Excel" button added next to Export');
console.log('OK  rows matched by name, columns by label; full plan shown before anything writes');
console.log('OK  blanks never clear; Person + Salesforce columns never written; unknown labels skipped');
console.log('Backup: spark-boards.html.bak-importxlsx');
