// CAL_BAND_v1 — the calendar becomes a proper full-width band directly under the
// KPI strip, at the top of Home, and it looks like a real product surface:
//   * day column with DOW + big date, TODAY called out in gold
//   * subject on its own line at 14px, then a meta row: start–end times,
//     location, a "Teams" pill, and the organizer
//   * an in-progress meeting gets a highlighted row
//   * a real "Join" button when the event carries a Teams link
//   * all-day events get their own pill instead of a fake time
//   * shimmer skeleton while loading; honest empty and error states
//   * footer shows "+N more in the next 7 days"
// ALSO fixes the slow load: previously the page made TWO /calendar round-trips
// (Today card + Pulse month). Now there is ONE fetch, shared by the band, the
// Today card, and the Pulse month grid.
// Scope unchanged: the worker serves only the signed-in user's own calendar.
// CRLF-safe, idempotent. Run from the repo root, then commit + push.
const fs = require("fs");
const F = "spark-home.html";
const raw = fs.readFileSync(F, "utf8");
if (raw.includes("CAL_BAND_v1")) { console.log("Already applied."); process.exit(0); }
if (!raw.includes("PULSE_CAL_LIVE_v1")) { console.error("ABORT — PULSE_CAL_LIVE_v1 not present"); process.exit(1); }
const hadCRLF = /\r\n/.test(raw);
let h = raw.replace(/\r\n/g, "\n");
function die(m){ console.error("ABORT — " + m + " (no changes written)"); process.exit(1); }

const CSS = "\n/* CAL_BAND_v1 \u2014 full-width agenda band under the KPI strip */\n.calband{margin-top:14px;background:linear-gradient(180deg,#fff,#fdfcf8);border:1px solid var(--line);border-radius:14px;box-shadow:0 10px 28px -16px rgba(20,18,10,.22);overflow:hidden;}\n.calband .cbhead{display:flex;align-items:center;gap:10px;padding:13px 18px;border-bottom:1px solid var(--line-light);}\n.calband .cbhead svg{width:15px;height:15px;fill:none;stroke:var(--gold-deep);stroke-width:1.9;}\n.calband .cbtitle{font-size:11px;letter-spacing:.13em;text-transform:uppercase;color:var(--muted);font-weight:600;}\n.calband .cbwho{margin-left:auto;font-size:11px;color:var(--muted);letter-spacing:.04em;}\n.calband .cbbody{display:grid;grid-template-columns:1fr;}\n.calband .cbrow{display:flex;gap:14px;align-items:flex-start;padding:12px 18px;border-bottom:1px solid var(--line-light);transition:background .12s;}\n.calband .cbrow:last-child{border-bottom:0;}\n.calband .cbrow:hover{background:#fffdf4;}\n.calband .cbrow.is-now{background:linear-gradient(90deg,#fffbe9,#fff 60%);}\n.calband .cbrow.is-now .cbtime b{color:#9a6b00;}\n.calband .cbday{min-width:52px;text-align:center;padding-top:2px;}\n.calband .cbday .dow{display:block;font-size:9.5px;letter-spacing:.1em;color:var(--muted);text-transform:uppercase;}\n.calband .cbday .dnum{display:block;font-size:20px;font-weight:700;line-height:1.05;color:#241f14;font-family:var(--font-display,inherit);}\n.calband .cbday.today .dnum{color:#8a5c00;}\n.calband .cbday.today .dow{color:#b98600;font-weight:700;}\n.calband .cbmain{flex:1;min-width:0;}\n.calband .cbsub{font-size:14px;font-weight:600;color:#241f14;line-height:1.35;margin-bottom:3px;overflow:hidden;text-overflow:ellipsis;}\n.calband .cbtime{font-size:11.5px;color:#6f6a5c;display:flex;flex-wrap:wrap;gap:5px;align-items:center;}\n.calband .cbtime b{font-weight:700;color:#3b3527;}\n.calband .cbtime .dot{color:var(--line);}\n.calband .cbpill{display:inline-flex;align-items:center;gap:4px;background:#f3f1e8;border:1px solid var(--line);color:#5d5850;border-radius:999px;padding:1px 7px;font-size:10px;letter-spacing:.03em;}\n.calband .cbpill.allday{background:#fff3c2;border-color:#f0e2a6;color:#6b5900;}\n.calband .cbjoin{flex-shrink:0;align-self:center;display:inline-flex;align-items:center;gap:5px;background:var(--gold);color:#412402;font-weight:700;font-size:11px;border-radius:8px;padding:6px 11px;text-decoration:none;white-space:nowrap;border:1px solid #e8bb17;}\n.calband .cbjoin:hover{filter:brightness(1.05);}\n.calband .cbempty{padding:22px 18px;font-size:13px;color:var(--muted);}\n.calband .cbfoot{padding:9px 18px;border-top:1px solid var(--line-light);font-size:11px;color:var(--muted);display:flex;gap:8px;align-items:center;}\n.calband .cbskel{padding:12px 18px;}\n.calband .cbskel div{height:11px;border-radius:6px;background:linear-gradient(90deg,#f3f1ea,#faf9f4,#f3f1ea);background-size:200% 100%;animation:cbsh 1.1s linear infinite;margin:7px 0;}\n@keyframes cbsh{0%{background-position:200% 0}100%{background-position:-200% 0}}\n@media(max-width:820px){.calband .cbrow{padding:11px 13px;gap:10px}.calband .cbsub{font-size:13px}}\n";
const li = h.lastIndexOf("</style>");
if (li < 0) die("no </style> found");
h = h.slice(0, li) + CSS + h.slice(li);

const P = [["MARKUP", "    </section>\n    <section class=\"dash\" id=\"dashPage\">", "    </section>\n    <section class=\"calband\" id=\"calBand\">\n      <div class=\"cbhead\">\n        <svg viewBox=\"0 0 24 24\"><rect x=\"3\" y=\"4\" width=\"18\" height=\"18\" rx=\"2\"/><path d=\"M16 2v4M8 2v4M3 10h18\"/></svg>\n        <span class=\"cbtitle\">Your next few days</span>\n        <span class=\"cbwho\" id=\"cbWho\">Outlook &middot; live</span>\n      </div>\n      <div class=\"cbbody\" id=\"cbBody\"><div class=\"cbskel\"><div style=\"width:62%\"></div><div style=\"width:44%\"></div><div style=\"width:71%\"></div></div></div>\n      <div class=\"cbfoot\" id=\"cbFoot\" style=\"display:none;\"></div>\n    </section>\n    <section class=\"dash\" id=\"dashPage\">"], ["FETCH", "wGet('/calendar').then(function(d){\n window.__CALDAY=d;\n var el=document.getElementById('todayLiveList');\n if(el)el.innerHTML=todayRows(d);\n}).catch(function(e){\n var el=document.getElementById('todayLiveList');\n if(el)el.innerHTML='<div class=\"sub\">Calendar unavailable right now</div>';\n console.log('[Home] /calendar unavailable:',e);\n});", "/* CAL_BAND_v1 \u2014 ONE calendar fetch shared by the band, the Today card and the\n   Pulse month grid (previously two separate round-trips through the worker). */\nfunction calEsc(s){return String(s==null?'':s).replace(/&/g,'&amp;').replace(/</g,'&lt;').replace(/>/g,'&gt;').replace(/\"/g,'&quot;');}\nfunction calFmt(d){var h=d.getHours(),m=d.getMinutes();var ap=h>=12?'PM':'AM';h=h%12;if(h===0)h=12;return h+(m?':'+(m<10?'0':'')+m:'')+' '+ap;}\nfunction calBandRender(d){\n  var body=document.getElementById('cbBody'), foot=document.getElementById('cbFoot');\n  if(!body) return;\n  var evs=((d&&d.events)||[]).filter(function(e){return e.start;});\n  var now=new Date(), t0=new Date(now.getFullYear(),now.getMonth(),now.getDate());\n  var horizon=new Date(t0.getTime()+7*864e5);\n  var rows=evs.filter(function(e){var s=new Date(e.start);return (e.allDay? s>=t0 : new Date(e.end||e.start)>=now) && s<horizon;})\n              .sort(function(a,b){return new Date(a.start)-new Date(b.start);});\n  if(!rows.length){ body.innerHTML='<div class=\"cbempty\">Nothing on your calendar for the next few days.</div>'; if(foot)foot.style.display='none'; return; }\n  var DOW=['SUN','MON','TUE','WED','THU','FRI','SAT'];\n  var show=rows.slice(0,6), out='', lastKey='';\n  show.forEach(function(e){\n    var s=new Date(e.start), en=e.end?new Date(e.end):null;\n    var key=s.toDateString(), isToday=(key===now.toDateString());\n    var dayCell='';\n    if(key!==lastKey){\n      dayCell='<div class=\"cbday'+(isToday?' today':'')+'\"><span class=\"dow\">'+(isToday?'TODAY':DOW[s.getDay()])+'</span><span class=\"dnum\">'+s.getDate()+'</span></div>';\n      lastKey=key;\n    } else { dayCell='<div class=\"cbday\"></div>'; }\n    var isNow = en && s<=now && en>=now;\n    var meta='';\n    if(e.allDay){ meta='<span class=\"cbpill allday\">All day</span>'; }\n    else { meta='<b>'+calFmt(s)+(en?' &ndash; '+calFmt(en):'')+'</b>'; }\n    var loc=e.location?String(e.location):'';\n    var isTeams=/teams/i.test(loc)||!!e.online;\n    if(loc && !isTeams) meta+='<span class=\"dot\">&middot;</span><span>'+calEsc(loc)+'</span>';\n    if(isTeams) meta+='<span class=\"dot\">&middot;</span><span class=\"cbpill\">Teams</span>';\n    if(e.organizer) meta+='<span class=\"dot\">&middot;</span><span>'+calEsc(e.organizer)+'</span>';\n    var join=(e.online&&/^https:\\/\\//.test(e.online))?'<a class=\"cbjoin\" href=\"'+calEsc(e.online)+'\" target=\"_blank\" rel=\"noopener\">Join</a>':'';\n    out+='<div class=\"cbrow'+(isNow?' is-now':'')+'\">'+dayCell+'<div class=\"cbmain\"><div class=\"cbsub\">'+calEsc(e.subject||'(no subject)')+'</div><div class=\"cbtime\">'+meta+'</div></div>'+join+'</div>';\n  });\n  body.innerHTML=out;\n  if(foot){\n    var extra=rows.length-show.length;\n    foot.innerHTML=(extra>0?('+'+extra+' more in the next 7 days'):('&nbsp;'))+(d&&d.count?('<span style=\"margin-left:auto;\">'+d.count+' events loaded</span>'):'');\n    foot.style.display='flex';\n  }\n}\nwGet('/calendar?days=45').then(function(d){\n window.__CALDAY=d; window.__CALMONTH=d;\n calBandRender(d);\n var el=document.getElementById('todayLiveList');\n if(el)el.innerHTML=todayRows(d);\n if(typeof pulseCalRender==='function') pulseCalRender(d);\n}).catch(function(e){\n var b=document.getElementById('cbBody');\n if(b)b.innerHTML='<div class=\"cbempty\">Calendar unavailable right now.</div>';\n var el=document.getElementById('todayLiveList');\n if(el)el.innerHTML='<div class=\"sub\">Calendar unavailable right now</div>';\n console.log('[Home] /calendar unavailable:',e);\n});"], ["PULSE_DEDUPE", "if(window.__CALDAY) pulseCalRender(window.__CALDAY);\nwGet('/calendar?days=45').then(function(d){ window.__CALMONTH=d; pulseCalRender(d); }).catch(function(e){\n  var list=document.getElementById('pulseCalEvents');\n  if(list) list.innerHTML='<div class=\"calevt\"><span>Calendar unavailable right now</span></div>';\n  console.log('[Pulse] calendar unavailable:',e);\n});", "/* CAL_BAND_v1: the shared fetch above drives this; no second round-trip */\nif(window.__CALMONTH) pulseCalRender(window.__CALMONTH);"]];
for (const [name, oldS, newS] of P) {
  const n = h.split(oldS).length - 1;
  if (n !== 1) die(name + " anchor found " + n + " times (want 1)");
  h = h.split(oldS).join(newS);
}
const stamp = new Date().toISOString().replace(/[:.]/g, "-");
fs.writeFileSync("spark-home.html.calband-" + stamp + ".bak", raw);
fs.writeFileSync(F, hadCRLF ? h.replace(/\n/g, "\r\n") : h);
console.log("APPLIED CAL_BAND_v1");
console.log("  calendar band now sits at the top of Home, under the KPI strip");
console.log("  end times, Teams pills, Join buttons, organizer, all-day handling");
console.log("  one shared /calendar fetch instead of two (faster load)");
console.log("  NEXT: git add spark-home.html && git commit -m \"Home: calendar band at top, richer detail, single fetch\" && git push");
