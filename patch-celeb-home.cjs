/* patch-celeb-home.cjs — move the Celebrations card to the HOME page and make it look right.
   1. Removes the v1 card script (it was anchored to lmsWelcomeBar, which lives on the Training tab,
      so it never appeared on the home page at all - my mistake).
   2. Parks the Pulse announcements block (Most recognized / Values in action strip + the
      Recognition/News/Milestones/Polls lanes) behind display:none. Hidden, not deleted: remove the
      two style="display:none" attributes on #pulseBoard and #pulseFeed to bring it back.
   3. Mounts a redesigned card in its place: today's celebrations in a highlighted band, then two
      columns for the whole month's birthdays and anniversaries. Passed dates dimmed, today's date
      badged in gold. SVG icons, your fonts and gold, responsive to one column on narrow screens.
   Run from repo root:  node patch-celeb-home.cjs
*/
const fs = require('fs');
const F = 'index.html';
function read(f){ if(!fs.existsSync(f)) throw new Error('Cannot find '+f+' - run from the repo root.'); return fs.readFileSync(f,'utf8').replace(/\r\n/g,'\n'); }
function repl(s, oldStr, newStr, label){
  const n = s.split(oldStr).length - 1;
  if(n !== 1) throw new Error('ANCHOR '+label+': expected 1 match, found '+n+'. index.html differs from the copy I patched against. Nothing written.');
  return s.replace(oldStr, () => newStr);
}
let x = read(F);
if (x.indexOf('HQ_CELEBRATIONS_v2') !== -1) throw new Error('Already applied. Aborting.');

/* strip the v1 card script (whole <script> block) if present */
(function(){
  const i = x.indexOf("<script>/*HQ_CELEBRATIONS_v1");
  if (i === -1) { console.log('note: v1 card script not found (already removed?) - continuing'); return; }
  const j = x.indexOf('</script>', i);
  if (j === -1) throw new Error('v1 card script has no closing tag; aborting rather than guess.');
  x = x.slice(0, i) + x.slice(j + 9);
  console.log('OK  removed the v1 card that was anchored to the Training tab');
})();
x = repl(x, "          <div class=\"pulse-board\" id=\"pulseBoard\"></div>\n          <div class=\"pulse-feed\" id=\"pulseFeed\"></div>", "          <!-- PULSE_HIDDEN_v1 — announcements block parked. Delete the two style=\"display:none\" attributes to bring it back. -->\n          <div class=\"pulse-board\" id=\"pulseBoard\" style=\"display:none\"></div>\n          <div class=\"pulse-feed\" id=\"pulseFeed\" style=\"display:none\"></div>", "hide pulse announcements block");
x = repl(x, "          <div class=\"pulse-board\" id=\"pulseBoard\" style=\"display:none\"></div>", "          <!-- HQ_CELEBRATIONS_v2 — birthdays & anniversaries, home page -->\n          <div class=\"celeb-wrap\" id=\"celebWrap\"></div>\n          <div class=\"pulse-board\" id=\"pulseBoard\" style=\"display:none\"></div>", "mount point on home page");
x = repl(x, "<style>/*HIDE_SL_MATCH_v1*/", "<style>/*HQ_CELEBRATIONS_v2*/\n.celeb-wrap{margin:0 0 18px}\n.celeb-card{background:linear-gradient(180deg,#fffdf7,#fff);border:1px solid var(--line);border-radius:16px;overflow:hidden;box-shadow:0 2px 14px rgba(23,22,20,.05)}\n.celeb-head{display:flex;align-items:baseline;gap:10px;padding:16px 20px 12px;border-bottom:1px solid rgba(184,145,46,.14)}\n.celeb-eyebrow{font-family:var(--font-display);font-size:10.5px;font-weight:700;letter-spacing:.18em;text-transform:uppercase;color:#B8912E}\n.celeb-sub{font-size:11.5px;color:var(--muted-light);margin-left:auto;letter-spacing:.02em}\n.celeb-today{display:flex;flex-wrap:wrap;gap:10px;padding:14px 20px;background:linear-gradient(90deg,rgba(245,197,24,.10),rgba(245,197,24,0))}\n.celeb-hero{display:flex;align-items:center;gap:11px;font-size:15px;line-height:1.3}\n.celeb-hero strong{font-weight:700}\n.celeb-hero .ch-sub{font-size:12px;color:var(--muted);display:block;margin-top:1px}\n.celeb-cols{display:grid;grid-template-columns:1fr 1fr;gap:0}\n@media(max-width:760px){.celeb-cols{grid-template-columns:1fr}}\n.celeb-col{padding:14px 20px 16px}\n.celeb-col+.celeb-col{border-left:1px solid rgba(184,145,46,.14)}\n@media(max-width:760px){.celeb-col+.celeb-col{border-left:0;border-top:1px solid rgba(184,145,46,.14)}}\n.celeb-col-t{font-family:var(--font-display);font-size:10px;font-weight:700;letter-spacing:.15em;text-transform:uppercase;color:var(--muted);margin-bottom:9px}\n.celeb-row{display:flex;align-items:center;gap:10px;padding:5px 0;font-size:13.5px;line-height:1.4}\n.celeb-row .cr-day{flex:0 0 30px;font-family:var(--font-display);font-weight:700;font-size:12px;color:#B8912E;text-align:right}\n.celeb-row .cr-name{font-weight:600;color:var(--ink)}\n.celeb-row .cr-meta{color:var(--muted);font-size:12px}\n.celeb-row.is-past{opacity:.42}\n.celeb-row.is-today .cr-day{color:#171614;background:var(--gold);border-radius:6px;padding:1px 5px;text-align:center;flex:0 0 auto;min-width:30px}\n.celeb-ico{flex:0 0 auto;width:16px;height:16px}\n.celeb-empty{font-size:12px;color:var(--muted-light);padding:2px 0 4px}\n</style>\n<style>/*HIDE_SL_MATCH_v1*/", "card css");
x = repl(x, "<script>/*STD_UNDER_TRAINING_v1*/", "<script>/*HQ_CELEBRATIONS_v2 — home-page celebrations card*/\n(function () {\n  var WK = \"https://spark-hq-worker.sparkcompanies.workers.dev\";\n  var NS = \"http://www.w3.org/2000/svg\";\n  var D_CAKE = \"M4 21h16|M5 21v-6a2 2 0 0 1 2-2h10a2 2 0 0 1 2 2v6|M8 13V9|M12 13V8|M16 13V9|M8 6.4c0-1 1-1.5 1-2.4 0 .9 1 1.4 1 2.4a1 1 0 0 1-2 0Z|M15 6.4c0-1 1-1.5 1-2.4 0 .9 1 1.4 1 2.4a1 1 0 0 1-2 0Z\";\n  var D_AWARD = \"M12 3a5 5 0 1 0 0 10 5 5 0 0 0 0-10Z|M8.5 12.5 7 21l5-2.5L17 21l-1.5-8.5\";\n  function ico(d, color) {\n    var s = document.createElementNS(NS, \"svg\");\n    s.setAttribute(\"viewBox\", \"0 0 24 24\"); s.setAttribute(\"class\", \"celeb-ico\");\n    s.setAttribute(\"fill\", \"none\"); s.setAttribute(\"stroke\", color);\n    s.setAttribute(\"stroke-width\", \"1.7\"); s.setAttribute(\"stroke-linecap\", \"round\"); s.setAttribute(\"stroke-linejoin\", \"round\");\n    d.split(\"|\").forEach(function (p) { var e = document.createElementNS(NS, \"path\"); e.setAttribute(\"d\", p); s.appendChild(e); });\n    return s;\n  }\n  function esc(s) { return String(s == null ? \"\" : s).replace(/[&<>\"]/g, function (c) { return { \"&\": \"&amp;\", \"<\": \"&lt;\", \">\": \"&gt;\", '\"': \"&quot;\" }[c]; }); }\n  function ord(n) { var s = [\"th\", \"st\", \"nd\", \"rd\"], v = n % 100; return n + (s[(v - 20) % 10] || s[v] || s[0]); }\n  function el(tag, cls, html) { var d = document.createElement(tag); if (cls) d.className = cls; if (html != null) d.innerHTML = html; return d; }\n  function personRow(e, kind) {\n    var r = el(\"div\", \"celeb-row\" + (e.today ? \" is-today\" : e.past ? \" is-past\" : \"\"));\n    r.appendChild(el(\"span\", \"cr-day\", e.today ? \"today\" : ord(e.day).replace(/^(\\d+)/, \"$1\")));\n    r.appendChild(ico(kind === \"b\" ? D_CAKE : D_AWARD, e.past ? \"#c9c3b7\" : kind === \"b\" ? \"#D4A017\" : \"#B8912E\"));\n    var meta = kind === \"b\"\n      ? (e.company ? '<span class=\"cr-meta\"> · ' + esc(e.company) + \"</span>\" : \"\")\n      : '<span class=\"cr-meta\"> · ' + e.years + \" year\" + (e.years === 1 ? \"\" : \"s\") + (e.company ? \" with \" + esc(e.company) : \"\") + \"</span>\";\n    r.appendChild(el(\"div\", null, '<span class=\"cr-name\">' + esc(e.name) + \"</span>\" + meta));\n    return r;\n  }\n  function build(j) {\n    var card = el(\"div\", \"celeb-card\");\n    var head = el(\"div\", \"celeb-head\");\n    head.appendChild(el(\"div\", \"celeb-eyebrow\", \"Celebrations\"));\n    var n = (j.monthBirthdays || []).length + (j.monthAnniversaries || []).length;\n    head.appendChild(el(\"div\", \"celeb-sub\", (j.monthName || \"\") + \" &middot; \" + n + \" to celebrate\"));\n    card.appendChild(head);\n    /* today gets its own band so it reads first */\n    var t = (j.today || []);\n    if (t.length) {\n      var band = el(\"div\", \"celeb-today\");\n      t.forEach(function (e) {\n        var h = el(\"div\", \"celeb-hero\");\n        h.appendChild(ico(e.type === \"birthday\" ? D_CAKE : D_AWARD, e.type === \"birthday\" ? \"#D4A017\" : \"#B8912E\"));\n        h.appendChild(el(\"div\", null, e.type === \"birthday\"\n          ? \"<strong>Happy birthday, \" + esc(e.first) + \"</strong>\" + (e.company ? '<span class=\"ch-sub\">' + esc(e.company) + \"</span>\" : \"\")\n          : \"<strong>\" + esc(e.name) + \"</strong>\" + '<span class=\"ch-sub\">' + e.years + \" year\" + (e.years === 1 ? \"\" : \"s\") + (e.company ? \" with \" + esc(e.company) : \"\") + \"</span>\"));\n        band.appendChild(h);\n      });\n      card.appendChild(band);\n    }\n    var cols = el(\"div\", \"celeb-cols\");\n    var mb = (j.monthBirthdays || []), ma = (j.monthAnniversaries || []);\n    var c1 = el(\"div\", \"celeb-col\");\n    c1.appendChild(el(\"div\", \"celeb-col-t\", (j.monthName || \"This month\") + \" birthdays\"));\n    if (mb.length) mb.forEach(function (e) { c1.appendChild(personRow(e, \"b\")); });\n    else c1.appendChild(el(\"div\", \"celeb-empty\", \"None this month\"));\n    cols.appendChild(c1);\n    var c2 = el(\"div\", \"celeb-col\");\n    c2.appendChild(el(\"div\", \"celeb-col-t\", (j.monthName || \"This month\") + \" anniversaries\"));\n    if (ma.length) ma.forEach(function (e) { c2.appendChild(personRow(e, \"a\")); });\n    else c2.appendChild(el(\"div\", \"celeb-empty\", \"None this month\"));\n    cols.appendChild(c2);\n    card.appendChild(cols);\n    return card;\n  }\n  function mount(j) {\n    if (!j || !j.ok) return;\n    if (!((j.today || []).length || (j.monthBirthdays || []).length || (j.monthAnniversaries || []).length)) return;\n    var host = document.getElementById(\"celebWrap\");\n    if (!host || host.getAttribute(\"data-done\")) return;\n    host.setAttribute(\"data-done\", \"1\");\n    host.appendChild(build(j));\n  }\n  function load() {\n    window.SPARK_SB.getToken().then(function (tok) {\n      if (!tok) return;\n      return fetch(WK + \"/hq-celebrations\", { headers: { Authorization: \"Bearer \" + tok } })\n        .then(function (r) { return r.json(); }).then(mount);\n    }).catch(function () { /* never let this break the home page */ });\n  }\n  function go() {\n    var n = 0;\n    var iv = setInterval(function () {\n      n++;\n      if (document.getElementById(\"celebWrap\") && window.SPARK_SB && window.SPARK_SB.getToken) { clearInterval(iv); load(); }\n      else if (n > 60) clearInterval(iv);\n    }, 250);\n  }\n  if (document.readyState !== \"loading\") go(); else document.addEventListener(\"DOMContentLoaded\", go);\n})();\n</script>\n<script>/*STD_UNDER_TRAINING_v1*/", "card script");
fs.writeFileSync(F + '.bak-celebhome', read(F));
fs.writeFileSync(F, x);
console.log('OK  announcements block hidden (Most recognized strip + Recognition/News/Milestones/Polls lanes)');
console.log('OK  Celebrations card mounted on the home page where that block was');
console.log('OK  today highlighted; month birthdays and anniversaries side by side; past dates dimmed');
console.log('Backup: index.html.bak-celebhome');
