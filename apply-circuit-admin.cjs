// CIRCUIT_ADMIN_v1 — a full content editor for Spark Circuit inside HQ Admin.
//   * New "Circuit content" item in the Admin submenu (super-admin only).
//   * Edits stage names/taglines, step titles, paragraphs, headings, notes,
//     bullet + numbered lists, video refs, links, and checklists; all 8 core
//     values; and the Dub-or-Keep job posting.
//   * Edits are LOCAL until "Publish to team" writes them to app_content
//     (key: circuit_content) stamped with publishedAt/publishedBy — same flow
//     as the LMS publish you already use.
//   * Quizzes and the Worker Profile are deliberately NOT editable (assessment
//     integrity — everyone must be assessed on the same instrument).
//   * This patch touches index.html ONLY. Circuit itself is not modified and
//     keeps rendering its built-in content until the separate reader patch
//     (CIRCUIT_OVERRIDE) is applied — so this is safe to ship on its own.
// REQUIRES circuit-content-seed.json committed next to index.html.
// Run from the repo root, then commit + push.
const fs = require("fs");
const F = "index.html";
const raw = fs.readFileSync(F, "utf8");
if (raw.includes("CIRCUIT_ADMIN_v1")) { console.log("Already applied."); process.exit(0); }
if (!fs.existsSync("circuit-content-seed.json")) { console.error("ABORT — circuit-content-seed.json is not in this folder. Download it and put it beside index.html first."); process.exit(1); }
const hadCRLF = /\r\n/.test(raw);
let h = raw.replace(/\r\n/g, "\n");
function die(m){ console.error("ABORT — " + m + " (no changes written)"); process.exit(1); }
const P = [["SUBNAV", "<button class=\"admin-subnav-item\" data-admin-tab=\"people-admin\">\n            <span>People & roles</span><span class=\"badge-mini\" id=\"peopleCountBadge\"></span>\n          </button>", "<button class=\"admin-subnav-item\" data-admin-tab=\"people-admin\">\n            <span>People & roles</span><span class=\"badge-mini\" id=\"peopleCountBadge\"></span>\n          </button>\n          <button class=\"admin-subnav-item\" data-admin-tab=\"circuit-admin\">\n            <span>Circuit content</span>\n          </button>"], ["PANE", "<!-- HQ_ADMIN_TRIM_v1: Content&SOPs / Security / Billing stub buttons removed -->\n        <div class=\"admin-content\">", "<!-- HQ_ADMIN_TRIM_v1: Content&SOPs / Security / Billing stub buttons removed -->\n        <div class=\"admin-content\">\n\n          <!-- CIRCUIT_ADMIN_v1 PANE -->\n          <div class=\"admin-pane\" data-admin-pane=\"circuit-admin\" style=\"display:none;\">\n            <div class=\"admin-hero\">\n              <div class=\"eyebrow\">Spark Circuit &middot; Content</div>\n              <h2>New-hire training content. <span style=\"color:var(--muted-light);\">Edit it here, publish when ready.</span></h2>\n              <div class=\"dek\">Stage checklists, the eight core values, and the Dub-or-Keep job posting. Edits stay local until you press Publish &mdash; then every new hire sees them on their next load. Quizzes and the Worker Profile stay fixed in the build so everyone is assessed on the same thing.</div>\n            </div>\n            <div class=\"admin-section\">\n              <div class=\"admin-section-head\">\n                <h3>Circuit content</h3>\n                <div class=\"actions\">\n                  <span id=\"circPubState\" style=\"font-size:11.5px;color:#8a8578;align-self:center;margin-right:6px;\"></span>\n                  <button class=\"btn-secondary\" id=\"circPubBtn\" onclick=\"publishCircuitToTeam()\" style=\"background:#FFC800;border-color:#FFC800;color:#171614;font-weight:700;\">Publish to team</button>\n                  <button class=\"btn-secondary\" onclick=\"circuitExportJson()\">Export JSON</button>\n                  <button class=\"btn-secondary\" onclick=\"circuitResetLocal()\">Discard my edits</button>\n                </div>\n              </div>\n              <div id=\"circuitAdminBody\"><div class=\"dek\">Loading Circuit content&hellip;</div></div>\n            </div>\n          </div>\n"], ["TABREG", "['team-progress','Team progress'],\n    ['agents','Agents'", "['team-progress','Team progress'], ['circuit-admin','Circuit content'],\n    ['agents','Agents'"], ["ROLE", "adminTabs: ['agents', 'connections', 'analytics', 'knowledge', 'role', 'people-admin', 'content', 'security', 'billing', 'training-admin', 'team-progress', /* ADMIN_PROGRESS_PERMS_v1 */ 'career-paths', 'spark-standard-admin', 'tools-admin']", "adminTabs: ['agents', 'connections', 'analytics', 'knowledge', 'role', 'people-admin', 'content', 'security', 'billing', 'training-admin', 'team-progress', /* ADMIN_PROGRESS_PERMS_v1 */ 'career-paths', 'spark-standard-admin', 'tools-admin', 'circuit-admin' /* CIRCUIT_ADMIN_v1 */]"], ["JS", "</body>\n</html>", "<script>\n/* ==================== CIRCUIT_ADMIN_v1 ==================== */\n(function(){\n  var CIRC = null;            /* working copy */\n  var CIRC_SEED = null;       /* pristine seed for reset/diff */\n  var CIRC_DIRTY_KEY = 'spark_hq_circuit_dirty';\n  var CIRC_LOCAL_KEY = 'spark_hq_circuit_local';\n  var SEED_URL = 'circuit-content-seed.json';\n\n  function esc(s){ return String(s==null?'':s).replace(/&/g,'&amp;').replace(/</g,'&lt;').replace(/>/g,'&gt;').replace(/\"/g,'&quot;'); }\n  function dirty(on){ try{ if(on) localStorage.setItem(CIRC_DIRTY_KEY,'1'); else localStorage.removeItem(CIRC_DIRTY_KEY); }catch(e){} paintState(); }\n  function isDirty(){ try{ return localStorage.getItem(CIRC_DIRTY_KEY)==='1'; }catch(e){ return false; } }\n  function saveLocal(){ try{ localStorage.setItem(CIRC_LOCAL_KEY, JSON.stringify(CIRC)); }catch(e){} dirty(true); }\n\n  function paintState(){\n    var el=document.getElementById('circPubState'); if(!el) return;\n    if(isDirty()){ el.textContent='Unpublished edits'; el.style.color='#a3792e'; }\n    else if(CIRC && CIRC.publishedAt){ el.textContent='Published '+new Date(CIRC.publishedAt).toLocaleString()+(CIRC.publishedBy?' by '+CIRC.publishedBy:''); el.style.color='#8a8578'; }\n    else { el.textContent='Live build content'; el.style.color='#8a8578'; }\n  }\n\n  function load(){\n    var body=document.getElementById('circuitAdminBody'); if(!body) return;\n    fetch(SEED_URL).then(function(r){ if(!r.ok) throw new Error('seed '+r.status); return r.json(); }).then(function(seed){\n      CIRC_SEED = seed;\n      var local=null; try{ local=JSON.parse(localStorage.getItem(CIRC_LOCAL_KEY)||'null'); }catch(e){}\n      if(local && local.stages){ CIRC=local; render(); return; }\n      if(window.SPARK_SB && window.SPARK_SB.getContent){\n        window.SPARK_SB.getContent('circuit_content').then(function(pub){\n          CIRC = (pub && pub.stages) ? pub : JSON.parse(JSON.stringify(seed));\n          render();\n        }).catch(function(){ CIRC=JSON.parse(JSON.stringify(seed)); render(); });\n      } else { CIRC=JSON.parse(JSON.stringify(seed)); render(); }\n    }).catch(function(e){\n      body.innerHTML='<div class=\"dek\">Could not load Circuit content ('+esc(e.message)+'). The seed file circuit-content-seed.json must sit next to index.html in the repo.</div>';\n    });\n  }\n\n  function render(){\n    var body=document.getElementById('circuitAdminBody'); if(!body||!CIRC) return;\n    var html='';\n    html+='<div style=\"display:flex;gap:8px;margin-bottom:14px;flex-wrap:wrap;\">';\n    html+='<button class=\"btn-secondary circ-tabbtn\" data-ct=\"stages\" onclick=\"circTab(\\'stages\\')\">Stages &amp; steps</button>';\n    html+='<button class=\"btn-secondary circ-tabbtn\" data-ct=\"values\" onclick=\"circTab(\\'values\\')\">Core values</button>';\n    html+='<button class=\"btn-secondary circ-tabbtn\" data-ct=\"dub\" onclick=\"circTab(\\'dub\\')\">Dub-or-Keep job</button>';\n    html+='</div><div id=\"circTabBody\"></div>';\n    body.innerHTML=html;\n    circTab(window.__circTab||'stages');\n    paintState();\n  }\n\n  window.circTab=function(t){\n    window.__circTab=t;\n    try{ document.querySelectorAll('.circ-tabbtn').forEach(function(b){ b.style.background = (b.getAttribute('data-ct')===t)?'#FFC800':''; b.style.fontWeight=(b.getAttribute('data-ct')===t)?'700':''; }); }catch(e){}\n    var el=document.getElementById('circTabBody'); if(!el) return;\n    if(t==='values') el.innerHTML=renderValues();\n    else if(t==='dub') el.innerHTML=renderDub();\n    else el.innerHTML=renderStages();\n  };\n\n  function renderStages(){\n    var out='';\n    (CIRC.stages||[]).forEach(function(st,si){\n      out+='<div style=\"border:1px solid var(--line);border-radius:10px;margin-bottom:12px;overflow:hidden;\">';\n      out+='<div style=\"background:#faf9f6;padding:10px 14px;display:flex;justify-content:space-between;align-items:center;cursor:pointer;\" onclick=\"circToggle(\\'cs'+si+'\\')\">';\n      out+='<div><b>'+esc(st.num)+' &middot; '+esc(st.name)+'</b> <span class=\"sub\" style=\"color:#8a8578;\">'+((st.steps||[]).length)+' steps</span></div><div style=\"color:#8a8578;\">edit</div></div>';\n      out+='<div id=\"cs'+si+'\" style=\"display:none;padding:14px;\">';\n      out+=field('Stage name', 'stages.'+si+'.name', st.name);\n      out+=field('Tagline', 'stages.'+si+'.tag', st.tag, true);\n      out+=field('Celebration line', 'stages.'+si+'.celebrate', st.celebrate, true);\n      (st.steps||[]).forEach(function(sp,pi){\n        out+='<div style=\"border-top:1px solid var(--line-light);margin-top:12px;padding-top:12px;\">';\n        out+='<div style=\"display:flex;justify-content:space-between;align-items:center;cursor:pointer;\" onclick=\"circToggle(\\'cs'+si+'p'+pi+'\\')\"><b style=\"font-size:13px;\">'+esc(sp.title)+'</b><span class=\"sub\" style=\"color:#8a8578;\">'+((sp.checks||[]).length)+' checks</span></div>';\n        out+='<div id=\"cs'+si+'p'+pi+'\" style=\"display:none;padding-top:10px;\">';\n        out+=field('Step title','stages.'+si+'.steps.'+pi+'.title', sp.title);\n        (sp.blocks||[]).forEach(function(b,bi){\n          var path='stages.'+si+'.steps.'+pi+'.blocks.'+bi+'.x';\n          if(b.t==='p'||b.t==='h'||b.t==='note') out+=field(b.t==='h'?'Heading':(b.t==='note'?'Note':'Paragraph'), path, b.x, true);\n          else if(b.t==='list'||b.t==='steps') out+=listField((b.t==='steps'?'Numbered steps':'Bullet list')+' (one per line)', path, b.x);\n          else if(b.t==='video') out+=field('Video file / URL', path, b.x);\n          else if(b.t==='link') out+=field('Link text', path, b.x)+field('Link URL','stages.'+si+'.steps.'+pi+'.blocks.'+bi+'.href', b.href||'');\n          else if(b.t==='kv') out+='<div class=\"sub\" style=\"color:#8a8578;margin:6px 0;\">'+(b.x||[]).length+' label/value rows &mdash; edit via Export JSON for now</div>';\n        });\n        out+=listField('Checklist items (one per line)','stages.'+si+'.steps.'+pi+'.checks', sp.checks||[]);\n        out+='</div></div>';\n      });\n      out+='</div></div>';\n    });\n    return out;\n  }\n\n  function renderValues(){\n    var out='<div class=\"dek\" style=\"margin-bottom:12px;\">The eight cards new hires flip in Stage 03.</div>';\n    (CIRC.values||[]).forEach(function(v,i){\n      out+='<div style=\"border:1px solid var(--line);border-radius:10px;padding:14px;margin-bottom:10px;\">';\n      out+=field('Value '+(i+1)+' title','values.'+i+'.name', v.name);\n      out+=field('Body','values.'+i+'.body', v.body, true);\n      out+='</div>';\n    });\n    return out;\n  }\n\n  function renderDub(){\n    var j=(CIRC.dub&&CIRC.dub.job)||{};\n    var out='<div class=\"dek\" style=\"margin-bottom:12px;\">The job posting trainees mark up in the Dub-or-Keep exercise.</div>';\n    out+=field('Req number','dub.job.req', j.req);\n    out+=field('Job title','dub.job.title', j.title);\n    out+=field('Client','dub.job.client', j.client);\n    out+=field('Location','dub.job.loc', j.loc);\n    out+=field('Rate','dub.job.rate', j.rate);\n    out+=listField('Key skills (one per line)','dub.job.keys', j.keys||[]);\n    return out;\n  }\n\n  function field(label, path, val, multi){\n    var id='cf_'+path.replace(/\\./g,'_');\n    return '<div style=\"margin-bottom:10px;\"><label style=\"display:block;font-size:11px;letter-spacing:.06em;text-transform:uppercase;color:#8a8578;margin-bottom:4px;\">'+esc(label)+'</label>'+\n      (multi\n        ? '<textarea id=\"'+id+'\" rows=\"3\" style=\"width:100%;padding:8px 10px;border:1px solid var(--line);border-radius:8px;font:13px/1.5 inherit;\" oninput=\"circSet(\\''+path+'\\',this.value)\">'+esc(val)+'</textarea>'\n        : '<input id=\"'+id+'\" type=\"text\" value=\"'+esc(val)+'\" style=\"width:100%;padding:8px 10px;border:1px solid var(--line);border-radius:8px;font:13px inherit;\" oninput=\"circSet(\\''+path+'\\',this.value)\">')+\n      '</div>';\n  }\n  function listField(label, path, arr){\n    var id='cf_'+path.replace(/\\./g,'_');\n    return '<div style=\"margin-bottom:10px;\"><label style=\"display:block;font-size:11px;letter-spacing:.06em;text-transform:uppercase;color:#8a8578;margin-bottom:4px;\">'+esc(label)+'</label>'+\n      '<textarea id=\"'+id+'\" rows=\"'+Math.max(2,Math.min(10,(arr||[]).length+1))+'\" style=\"width:100%;padding:8px 10px;border:1px solid var(--line);border-radius:8px;font:13px/1.5 inherit;\" oninput=\"circSetList(\\''+path+'\\',this.value)\">'+esc((arr||[]).join('\\n'))+'</textarea></div>';\n  }\n\n  function walk(path){\n    var parts=path.split('.'), o=CIRC;\n    for(var i=0;i<parts.length-1;i++){ o=o[parts[i]]; if(o==null) return null; }\n    return {obj:o, key:parts[parts.length-1]};\n  }\n  window.circSet=function(path,val){ var t=walk(path); if(!t)return; t.obj[t.key]=val; saveLocal(); };\n  window.circSetList=function(path,val){ var t=walk(path); if(!t)return; t.obj[t.key]=val.split('\\n').map(function(s){return s.trim();}).filter(function(s){return s.length;}); saveLocal(); };\n  window.circToggle=function(id){ var e=document.getElementById(id); if(e) e.style.display = (e.style.display==='none'?'block':'none'); };\n\n  window.circuitExportJson=function(){\n    try{\n      var blob=new Blob([JSON.stringify(CIRC,null,1)],{type:'application/json'});\n      var a=document.createElement('a'); a.href=URL.createObjectURL(blob); a.download='circuit-content.json'; a.click();\n    }catch(e){ alert('Export failed: '+e.message); }\n  };\n  window.circuitResetLocal=function(){\n    if(!confirm('Discard your unpublished edits and reload the published (or build) content?')) return;\n    try{ localStorage.removeItem(CIRC_LOCAL_KEY); }catch(e){}\n    dirty(false); CIRC=null; load();\n  };\n\n  window.publishCircuitToTeam=function(){\n    if(!window.SPARK_SB || !window.SPARK_SB.setContent){ alert('Cloud sync is not available on this page.'); return; }\n    var who=null; try{ who=window.SPARK_SB.whoami(); }catch(e){}\n    if(!who||!who.email){ alert('Sign in first \u2014 publishing records who published.'); return; }\n    if(!CIRC||!CIRC.stages||!CIRC.stages.length){ alert('Nothing loaded to publish.'); return; }\n    var nSteps=(CIRC.stages||[]).reduce(function(a,s){return a+((s.steps||[]).length);},0);\n    if(!confirm('Publish Circuit content to every new hire?\\n\\n'+CIRC.stages.length+' stages / '+nSteps+' steps / '+(CIRC.values||[]).length+' values\\nAnyone who opens Circuit next will see this version.')) return;\n    var payload=JSON.parse(JSON.stringify(CIRC));\n    payload.v=1; payload.publishedAt=new Date().toISOString(); payload.publishedBy=who.email;\n    var btn=document.getElementById('circPubBtn'); if(btn){ btn.disabled=true; btn.textContent='Publishing\u2026'; }\n    window.SPARK_SB.setContent('circuit_content', payload).then(function(){\n      CIRC=payload; try{ localStorage.removeItem(CIRC_LOCAL_KEY); }catch(e){}\n      dirty(false);\n      if(btn){ btn.disabled=false; btn.textContent='Publish to team'; }\n      paintState(); alert('Published. New hires see this version on their next load.');\n    }).catch(function(e){\n      if(btn){ btn.disabled=false; btn.textContent='Publish to team'; }\n      alert('Publish failed: '+((e&&e.message)||e));\n    });\n  };\n\n  /* load when the pane is first opened */\n  document.addEventListener('click', function(ev){\n    var b=ev.target && ev.target.closest && ev.target.closest('[data-admin-tab=\"circuit-admin\"]');\n    if(b && !CIRC) setTimeout(load, 60);\n  }, true);\n})();\n\n</script>\n</body>\n</html>"]];
for (const [name, oldS, newS] of P) {
  const n = h.split(oldS).length - 1;
  if (n !== 1) die(name + " anchor found " + n + " times (want 1)");
  h = h.split(oldS).join(newS);
}
const stamp = new Date().toISOString().replace(/[:.]/g, "-");
fs.writeFileSync("index.html.circadmin-" + stamp + ".bak", raw);
fs.writeFileSync(F, hadCRLF ? h.replace(/\n/g, "\r\n") : h);
console.log("APPLIED CIRCUIT_ADMIN_v1 — Admin > Circuit content is live");
console.log("  Editable: stages/steps/checklists, 8 core values, Dub-or-Keep job");
console.log("  Publish writes app_content key 'circuit_content' (stamped who/when)");
console.log("  Circuit itself is UNCHANGED until the reader patch is applied");
console.log("  NEXT: git add index.html circuit-content-seed.json && git commit -m \"Circuit content admin (editor + publish)\" && git push");
