/* patch-celeb-month.cjs — /hq-celebrations also returns whole-month roll-ups.
   Adds monthBirthdays and monthAnniversaries: everyone whose birthday or work anniversary
   falls in the CURRENT calendar month, each flagged past/today so the card can dim the ones
   already gone by. Lookahead for the today/upcoming lists widens from 7 to 14 days.
   Run from repo root:  node patch-celeb-month.cjs
*/
const fs = require('fs');
const F = "worker/cloudworker.js";
function read(f){ if(!fs.existsSync(f)) throw new Error('Cannot find '+f+' - run from the repo root.'); return fs.readFileSync(f,'utf8').replace(/\r\n/g,'\n'); }
function repl(s, oldStr, newStr, label){
  const n = s.split(oldStr).length - 1;
  if(n !== 1) throw new Error('ANCHOR '+label+': expected 1 match, found '+n+'. File differs from the copy I patched against. Nothing written.');
  return s.replace(oldStr, () => newStr);
}
let x = read(F);
if (x.indexOf("monthBirthdays") !== -1) throw new Error('Already applied. Aborting.');
x = repl(x, "        const out = { today: [], upcoming: [] };\n        const HORIZON = 7;\n        (board.groups || []).forEach((g) => (g.items || []).forEach((it) => {\n          if (!it || it.deletedAt || it.archivedAt) return;\n          const np = hcName(it.name);\n          if (!np.first) return;\n          const b = cBirth ? hcParse(it[cBirth.key]) : null;\n          const h = cHire ? hcParse(it[cHire.key]) : null;\n          for (let n = 0; n <= HORIZON; n++) {\n            const day = hcAdd(hcToday, n);\n            const bucket = n === 0 ? \"today\" : \"upcoming\";\n            const push = (type, years) => {\n              const e = { name: np.display, first: np.first, company: coOf(it), type: type };\n              if (type === \"anniversary\") e.years = years;\n              if (bucket === \"upcoming\") { e.date = day.y + \"-\" + String(day.m).padStart(2, \"0\") + \"-\" + String(day.d).padStart(2, \"0\"); e.inDays = n; }\n              out[bucket].push(e);\n            };\n            if (b) { const md = hcObs(b, day.y); if (md.m === day.m && md.d === day.d) push(\"birthday\", null); }\n            if (h) { const md = hcObs(h, day.y); if (md.m === day.m && md.d === day.d) { const yrs = day.y - h.y; if (yrs >= 1) push(\"anniversary\", yrs); } }\n          }\n        }));\n        out.upcoming.sort((a, b) => a.inDays - b.inDays || a.name.localeCompare(b.name));", "        const out = { today: [], upcoming: [], monthBirthdays: [], monthAnniversaries: [] };\n        const HORIZON = 14;\n        const MON_NAMES = [\"January\",\"February\",\"March\",\"April\",\"May\",\"June\",\"July\",\"August\",\"September\",\"October\",\"November\",\"December\"];\n        (board.groups || []).forEach((g) => (g.items || []).forEach((it) => {\n          if (!it || it.deletedAt || it.archivedAt) return;\n          const np = hcName(it.name);\n          if (!np.first) return;\n          const b = cBirth ? hcParse(it[cBirth.key]) : null;\n          const h = cHire ? hcParse(it[cHire.key]) : null;\n          /* whole-month roll-ups: every birthday and work anniversary falling in the current\n             calendar month, regardless of whether it has already passed this month. */\n          if (b) {\n            const md = hcObs(b, hcToday.y);\n            if (md.m === hcToday.m) out.monthBirthdays.push({ name: np.display, first: np.first, company: coOf(it), day: md.d, past: md.d < hcToday.d, today: md.d === hcToday.d });\n          }\n          if (h) {\n            const md = hcObs(h, hcToday.y);\n            const yrs = hcToday.y - h.y;\n            if (md.m === hcToday.m && yrs >= 1) out.monthAnniversaries.push({ name: np.display, first: np.first, company: coOf(it), day: md.d, years: yrs, past: md.d < hcToday.d, today: md.d === hcToday.d });\n          }\n          for (let n = 0; n <= HORIZON; n++) {\n            const day = hcAdd(hcToday, n);\n            const bucket = n === 0 ? \"today\" : \"upcoming\";\n            const push = (type, years) => {\n              const e = { name: np.display, first: np.first, company: coOf(it), type: type };\n              if (type === \"anniversary\") e.years = years;\n              if (bucket === \"upcoming\") { e.date = day.y + \"-\" + String(day.m).padStart(2, \"0\") + \"-\" + String(day.d).padStart(2, \"0\"); e.inDays = n; }\n              out[bucket].push(e);\n            };\n            if (b) { const md = hcObs(b, day.y); if (md.m === day.m && md.d === day.d) push(\"birthday\", null); }\n            if (h) { const md = hcObs(h, day.y); if (md.m === day.m && md.d === day.d) { const yrs = day.y - h.y; if (yrs >= 1) push(\"anniversary\", yrs); } }\n          }\n        }));\n        out.upcoming.sort((a, b) => a.inDays - b.inDays || a.name.localeCompare(b.name));\n        out.monthBirthdays.sort((a, b) => a.day - b.day || a.name.localeCompare(b.name));\n        out.monthAnniversaries.sort((a, b) => a.day - b.day || a.name.localeCompare(b.name));\n        out.monthName = MON_NAMES[hcToday.m - 1];", "celebrations month scope");
x = repl(x, "        return new Response(JSON.stringify({ ok: true, asOf: asOf, board: hit.name, today: out.today, upcoming: out.upcoming }), {", "        return new Response(JSON.stringify({ ok: true, asOf: asOf, board: hit.name, monthName: out.monthName, today: out.today, upcoming: out.upcoming, monthBirthdays: out.monthBirthdays, monthAnniversaries: out.monthAnniversaries }), {", "celebrations response payload");
fs.writeFileSync(F + ".bak-monthBirthda", read(F));
fs.writeFileSync(F, x);
console.log("OK  /hq-celebrations now returns whole-month birthdays and anniversaries");
console.log("OK  each entry flagged past/today so passed dates render dimmed");
console.log("Backup: worker/cloudworker.js.bak-monthBirthda");
